# Get current branch
current_branch=$(git symbolic-ref --short HEAD)

# Only run version script if on pzen_2 branch
if [ "$current_branch" = "pzen_2" ]; then
  echo "Updating version for pzen_2 deployment..."
  node scripts/update-version.js
fi